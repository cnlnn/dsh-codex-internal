# Fixture provenance

The two payloads are sanitized reconstructions based on the observed errors at positions 3740 and 2978. They preserve the complete object shape and the malformed `arguments_json` tail ending at `,{`; they are not exact raw responses.
